name: miprimerworkflow
run-name: ${{github.actor}} is learning Github Actions
on : [push]
jobs:
  check-bats-version:
     runs_on: ubuntu-later
     steps:
     -users: actions/checkout@v4
     -users: actions/setup-node@v4
   whith:
     node-version: '20'
     -run: npm install -g bats
     -run: bats -v